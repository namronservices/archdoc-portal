standard_id: oauth2-client-credentials
title: OAuth2 Client Credentials
archimate_type: requirement
body: |
  Service-to-service authentication must use OAuth2 client-credentials flow
  with short-lived access tokens issued by the platform IdP.
